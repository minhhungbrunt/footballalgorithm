name: FootballEdge refresh

on:
  workflow_dispatch:
  schedule:
    - cron: "*/5 * * * *"

permissions:
  contents: write

concurrency:
  group: footballedge-refresh
  cancel-in-progress: true

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Sync main before update
        run: git pull --rebase origin main

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Pull FotMob
        run: python scripts/update.py

      - name: Validate generated feed
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          p=Path("data/fixtures.json")
          x=json.loads(p.read_text(encoding="utf-8"))
          assert isinstance(x.get("matches"), list) and x["matches"], "No fixtures generated"
          for m in x["matches"]:
              assert m.get("home") and m.get("away") and m.get("competition")
          print("VALID:", len(x["matches"]), "fixtures")
          lineups=sum(bool((m.get("homeData") or {}).get("lineup")) and bool((m.get("awayData") or {}).get("lineup")) for m in x["matches"])
          forms=sum(bool((m.get("homeData") or {}).get("form")) and bool((m.get("awayData") or {}).get("form")) for m in x["matches"])
          divisions=sum(bool((m.get("homeData") or {}).get("division")) and bool((m.get("awayData") or {}).get("division")) for m in x["matches"])
          xgs=sum((m.get("homeData") or {}).get("xg") is not None and (m.get("awayData") or {}).get("xg") is not None for m in x["matches"])
          print(f"ENRICHMENT: {lineups} lineups · {forms} form pairs · {divisions} divisions · {xgs} xG pairs")
          PY

      - name: Commit refreshed data
        run: |
          git config user.name "football-edge-bot"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/fixtures.json
          git add data/ml_model.joblib 2>/dev/null || true
          git diff --cached --quiet && echo "No data changes" && exit 0
          git commit -m "chore: refresh football data"
          git pull --rebase origin main
          git push origin HEAD:main
